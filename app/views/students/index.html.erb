<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Acceuil</a>
      
      <a class="d-flex" href="#">Ajouter un élève</a>
      </div>
    </div>
  </nav>



<%= notice %>
<div class="row">
    <div class="col-3">

    </div>
    <div class="col-6">
        <h1 class="text-center">Students List</h1> <br>
        <table class="table table-striped table-hover">
            <tr>
                <th>Last Name</th>
                <th>First Name</th>
                <th>Age</th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
        
            <% @students.each do |student|%>
        
                <tr>
                    <td> <%= student.last_name %> </td>
                    <td> <%= student.first_name %> </td>
                    <td> <%= student.age %> </td>
                    <td> <%= link_to 'Show', student_path(student.id) %> </td>
                    <td> <%= link_to 'Edit', edit_student_path(student.id) %> </td>
                    <td> <%= link_to 'Delete', student_path(student.id), method: :delete ,
                    data: {confirm: 'Êtes-vous sûr de vouloir supprimer ?'}%> </td>
                </tr>
        
            <% end %>
        </table>
        <%= link_to 'Ajouter un eleve', new_student_path%>
    </div>
    <div class="col-3"></div>
   